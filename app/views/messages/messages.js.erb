setTimeout(function() {
  window.location.href = "<%= messages_path %>";
}, 800);

$.notify({
  icon: 'glyphicon glyphicon-ok',
  message: '訊息發送成功，請耐心等待回覆',
},{
  type: 'success',
  delay: 3000,
});

