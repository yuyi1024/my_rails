<% if @cat2s.present? %>
  $('#cat2').html("<%= j render partial: 'result', locals: { cat2s: @cat2s } %>");
<% end %>

<% if @items.present? %>
  $('#item').html("<%= j render partial: 'item', locals: { items: @items } %>");
<% end %>